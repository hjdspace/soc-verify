// App Shell 几何常量（来源：docs/prototypes/app-shell-redesign-a2-mission-control-refined.html）
// 布局组件共用，避免各组件数值漂移。

/** TitleBar 高度（px） */
export const TITLE_BAR_HEIGHT = 40;

/** 全局状态栏高度（px） */
export const STATUS_BAR_HEIGHT = 24;

/** NavRail 宽度（60px 图标栏 + 1px 右边框） */
export const NAV_RAIL_WIDTH = 61;

/** 左抽屉停靠位：导航栏右侧留 7px 间隙（61 + 7 = 68） */
export const DRAWER_LEFT_OFFSET = 68;
